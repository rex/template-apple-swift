`make build-mac` additionally builds `{{app_name}}Mac`. macOS targets carry the
hardened runtime plus App Sandbox; both are required for Mac App Store
distribution and for notarised Developer ID builds.
