# MAP.md — module map for {{display_name}}

<!-- Where things live and who owns what. Generated {{date}}; edit freely. -->

## Targets

{{targets_table}}

Every extension bundle ID is its host's ID plus exactly one segment. A
two-segment extension ID is an ITMS-90347 rejection at upload time, not a
build error, so it is checked in YAML rather than discovered by App Review.
